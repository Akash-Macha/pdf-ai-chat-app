import { createTheme } from '@mantine/core';

export const theme = createTheme({
  primaryColor: 'teal',
  fontFamily: 'Sora, sans-serif',
  headings: {
    fontFamily: 'Sora, sans-serif',
    fontWeight: '700',
  },
  defaultRadius: 'md',
});
